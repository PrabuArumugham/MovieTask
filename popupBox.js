import React from 'react';
import Popup from "reactjs-popup";
const popupBox = (props) => {
    let but='filter Method';
    return (
        <div>
          Hissdsdsdsdsdd{alert(props.name)}
        </div>
    );
}

export default popupBox;
